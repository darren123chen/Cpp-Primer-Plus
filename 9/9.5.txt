1.a. 自动变量；
  b. 外部链接的静态变量；
  c. 内部链接的静态变量；
  d. 无链接的静态变量。

2.using编译指令相当于大量使用了作用域解析符（::），和使用多个using声明不一样。使用using 声明时，会声明一个该变量，作用域内不允许声明同名变量。而使用using编译指令相当于使用作用域解析符，作用域内声明局部变量将隐藏名称空间中的同名变量。

一般来说，使用using声明比使用using编译指令更安全。

4. 内部链接。
