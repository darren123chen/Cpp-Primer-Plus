1. 类是用户定义的类型的定义。
2. 抽象：类表示人们可以类方法的共有借口对类对象执行的操作。
   数据隐藏：类的数据成员可以是私有的，只能通过成员函数来访问这些变量。
   封装：实现的具体方法都是隐藏的。
3. 类定义了一种类型，包括如何使用。对象是类的实例。
4. 不同对象各自有自己的数据内存空间；但所有对象都使用同一组成员函数。
6. 1.初始化或显式调用时；
   2.对象过期时。
8. 没有参数或所有参数都有默认值的构造函数。拥有默认构造函数后，可以声明对象而不初始化它（隐式初始化）。能够声明数组。

10. this是指针，*this是对象。
